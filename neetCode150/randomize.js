// 17 Sections (start at zero)
// https://neetcode.io/

function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

console.log(getRandomInt(7));

// 5/12/22 -> Section 3: Heap / Priority Queue -> Problem: Task Scheduler
