// Maximum Distinct

function getMaximumDistinctCount(a, b, k) {
  // Write your code here

  return "Testing";
}

let a = [2, 3, 3, 2, 2];
let b = [1, 3, 2, 4, 1];
let k = 2;

console.log(getMaximumDistinctCount(a, b, k));
