let lengths = [26, 103, 59];
let costPerCut = 1;
let salePrice = 10;

const maxProfit = (costPerCut, salePrice, lengths) => {
    // (costPerCut * (numOfRods) * )
  return;
};

console.log(maxProfit(costPerCut, salePrice, lengths));
